{
  "name": "godot-mcp",
  "version": "0.1.0",
  "description": "MCP server connecting Godot Engine with Claude Code",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "release": "bash scripts/build-release.sh"
  },
  "keywords": ["godot", "mcp", "claude"],
  "license": "MIT",
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "zod": "^3.24.0"
  },
  "dependencies": {}
}
